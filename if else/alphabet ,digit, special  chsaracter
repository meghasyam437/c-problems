#include <stdio.h>

int main() {
    char ch;
    printf("Enter a character: ");
    scanf("%c", &ch);
    if(ch>=65&&ch<=90||ch>=97&&ch<=122)
    printf("alphabets");
    else if(ch>=48&&ch<=57)
    printf("digits");
else
printf("special character");
    return 0;
}
