#include <stdio.h>

int main() {
    int attendance;
    scanf("%d",&attendance);
    if(attendance>=75)
    printf("eligible for exam");
    if(attendance<75)
    printf("not eligible");
    

    return 0;
}
