#include <stdio.h>

int main() {
    int a,b,c,result;
    scanf("%d %d %d",&a,&b,&c);
    int d=a+b+c;
    printf("%d",d);
    if(result=180)
    printf("valid");
    else
    printf("invalid");
    return 0;
}
