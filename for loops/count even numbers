#include <stdio.h>
int main() {
  int n,count=0,digits;
  scanf("%d",&n);
  for(;n>0;n/=10){
    digits=n/10;
    if(digits%2==0){
        count++;
    }
  }  //Your Code goes here!
    printf("%d",count);
    return 0;
}
