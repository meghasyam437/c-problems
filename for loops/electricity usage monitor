#include <stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    int units;
    int total;
    for(int i=0;i<n;i++){
        scanf("%d",&units);
        total+=units;
    }
    printf("%d",total);
    return 0;
}
