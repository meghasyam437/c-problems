#include <stdio.h>
int main() {
  int n;
  int expense;
  int total=0,overspenddays=0;
  scanf("%d",&n);
  for(int i=0;i<n;i++){
    scanf("%d",&expense);
    total+=expense;
    if(expense>100){
        overspenddays++;
    }
  }
printf("%d %d",total,overspenddays);
return 0;
}
