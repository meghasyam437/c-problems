#include <stdio.h>

int main() {
    int n,per_day;
    scanf("%d",&n);
    int total=0,heavy_rain_days=0;
    for(int i=0;i<n;i++){
        scanf("%d",&per_day);
        total+=per_day;
if(per_day>50)
  heavy_rain_days++;
    }
    printf("%d %d",total,heavy_rain_days);

    return 0;
}
