#include <stdio.h>

int main() {
    int n,daily_loss;
    scanf("%d",&n);
int total_loss=0,high_loss_days=0;
for(int i=0;i<n;i++){
    scanf("%d",&daily_loss);
    total_loss+=daily_loss;
    if(daily_loss>100)
    high_loss_days++;
}
printf("%d %d",total_loss,high_loss_days);  
    return 0;
}
