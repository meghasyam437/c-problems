#include <stdio.h>
int main() {
  int room_type,fee;
  scanf("%d",&room_type);
  switch(room_type){
    case 1:
    fee=80000;
    break;
    case 2:
    fee=60000;
     break;
     case 3:
     fee=45000;
     break;
     default:
     printf("invalid room_type");
  }
  printf("%d",fee);
    return 0;
}
