#include <stdio.h>
int main() {
  int user_role,login_time;
  scanf("%d %d",&user_role,&login_time);
  switch(user_role){
    case 1:
    printf("full access at any time");
    break;
    case 2:
    if(login_time>=9&&login_time<=18)
    printf("limited access");
    else
    printf("access denied");
    break;
}
    return 0;
}
