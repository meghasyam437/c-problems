#include <stdio.h>
int main() {
int n,expected_sum,missing,sum;
scanf("%d",&n);
int arr[n-1];
for(int i=0;i<n-1;i++){
scanf("%d",&arr[i]);
sum+=arr[i];
}
expected_sum=n*(n+1)/2;
missing=expected_sum-sum;
printf("%d",missing);

    return 0;
}
